# Support feed schema

This repository publishes one device profile only:

- `r12s-S721BXXSCDZF3`
- Model: `SM-S721B`
- Kernel: `6.1.157`
- Android SDK: `36` / Android 16

`targets-v3.json` is the current feed. `targets-v2.json` keeps the same
S721B profile for older 0.2.3 clients. No other device models are listed.
